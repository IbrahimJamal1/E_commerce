import 'package:flutter/material.dart';

class OrderState extends StatefulWidget {
  const OrderState({super.key});
  @override
  State<OrderState> createState() => __OrderSStateState();
}

class __OrderSStateState extends State<OrderState> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("state Order"),),
    );
  }
}